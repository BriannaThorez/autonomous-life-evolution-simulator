const e='# TASK_001: Meta-Procedural Task Synthesizer\n\nThis document explains the logic and execution of the `TASK_001_MASTER_BUILDER` procedural instruction file. This task acts as the master template for generating *more* task procedures, ensuring all future operations inherit the rigorous, meaning-preserving DNA of the Genesis Protocol.\n\n## 1. Protocol Invocation\n**Code:**\n```yaml\nprotocol_id: "[SYS_AXIOM_UNIFIED]"\ntask_id: "TASK_001_MASTER_BUILDER"\n```\n**Explanation:** \nThe agent reads `[SYS_AXIOM_UNIFIED]`. This triggers a context-switch, forcing the agent to abandon standard conversational probability and adopt the strict formal refinement engine rules defined in its system prompt. It recognizes `TASK_001` as a meta-task—a tool designed to build other tools.\n\n## 2. Axiomatic Intent (The Mathematical Boundary)\n**Code:**\n```yaml\naxiomatic_intent: "∀U_prompt ⊢ I, generate T_new ≣ f(I, Π) where T_new ⊆ [SYS_AXIOM_UNIFIED] ∧ Δ_drift(T_new) ≡ 0."\n```\n**Explanation:** \n"For every user prompt provided (`∀U_prompt`), extract the absolute core intent (`⊢ I`). Use this intent to generate a new Task (`T_new`). This new task must be a strict function of the intent and the invariant policy (`f(I, Π)`). The resulting task must inherit the rules of the unified protocol (`⊆ [SYS_AXIOM_UNIFIED]`) and must contain absolutely zero semantic deviation from the user\'s request (`Δ_drift ≡ 0`)."\n\n## 3. Axiological Intent (The Subjective Value)\n**Code:**\n```yaml\naxiological_intent: "To endow the development lifecycle with a self-replicating, mathematically rigorous framework that preserves the profound meaning and structural integrity of the cognitive simulation across all future iterations."\n```\n**Explanation:** \nThe agent contextualizes the math. It understands that it is not just generating random YAML files, but is actively constructing a "self-replicating framework." This ensures that the generated language in the new task maintains a tone of profound meaning and structural integrity, aligning with the "soul" of the simulation rather than reading like sterile boilerplate.\n\n## 4. Formal Proof (The Execution Guarantee)\n**Code:**\n```yaml\nformal_proof:\n  premise_1: "(T_n ⊢ T_{n+1}) ∧ (T_n ≡ CONST) ⇒ T_{n+1} ⊆ [SYS_AXIOM_UNIFIED]"\n  premise_2: "Δ_drift(T_{n+1}) ≡ 0 ∧ (V_Δ · V_cliche ≤ 1 - W_c)"\n  conclusion: "T_new ≣ VALID ∧ Sem(T_new) ≣ Sem(U)"\n```\n**Explanation:** \nBefore executing, the agent validates the logic of what it is about to do:\n*   **`premise_1`:** If the current master task (`T_n`) generates the next task (`T_{n+1}`), and the rules of the master task remain constant, then the new task is mathematically guaranteed to belong to the same rigorous protocol.\n*   **`premise_2`:** The new task must have zero drift, and its logical vector (`V_Δ`) must be orthogonal to standard, cliché LLM responses (`V_cliche`), scaled by the requested complexity weight (`W_c`).\n*   **`conclusion`:** If the premises hold, the generated task is valid, and its semantic meaning perfectly mirrors the user\'s universe/request.\n\n## 5. Procedural Logic (The Execution Loop)\n**Code:**\n```yaml\nprocedural_logic:\n  step_1: "U ⊢ I : Extract axiomatic specification (I) from Universe (U)."\n  step_2: "Define Π ≜ [Scope(I) ≣ Δ ∧ (U \\\\ I) ≡ CONST] : Map boundaries of constant state space."\n  step_3: "Execute ☰(P) : Architect solution path P where logic of Δ ⊥ standard boilerplate relative to W_c."\n  step_4: "P ⇒ R : Synthesize deterministic projection R formatted as YAML ∈ app_manifest/tasks/."\n  step_5: "Verify (Sem(R) ≣ Sem(U)) ∧ (E_code \\\\ E_manifest ≡ ∅) ∧ (Π ∪ (U, I) ⊢ P ≣ COMPLETE)."\n```\n**Explanation:** \nThis is the step-by-step algorithm the agent executes to fulfill the user\'s request:\n*   **`step_1`:** The agent parses the user\'s request. It strips away conversational filler to isolate the axiomatic specification (`I`).\n*   **`step_2`:** The agent defines the policy (`Π`). It determines exactly what needs to be created (`Δ`) and locks the rest of the system state as an immutable constant (`CONST`).\n*   **`step_3`:** The agent invokes the Qian operator (`☰`). It architects the formal proof (`P`) for the new task. If the agent detects it is generating generic boilerplate, the Qian operator forces a recursive recalibration until the logic is highly dense and specific to the simulation.\n*   **`step_4`:** The agent synthesizes the formal proof (`P`) into the final projection (`R`). It formats this projection strictly as a YAML file and outputs it to the `app_manifest/tasks/` directory.\n*   **`step_5`:** The Tripartite Gate. Before concluding the turn, the agent checks its own work: Does the YAML file mean exactly what the user asked? Were there any manual structural errors? Is the proof complete?\n\n## 6. Look Ahead (The Terminal State)\n**Code:**\n```yaml\nlook_ahead: "T_{n+1} serves as an immutable, contextually bound instruction set for the execution engine, enabling recursive, zero-drift expansion of the simulation\'s cognitive architecture."\n```\n**Explanation:** \nThe agent defines the future utility of the output. It recognizes that the newly generated task (`T_{n+1}`) is now an immutable instruction set. The execution loop closes, and the system is ready for the next prompt, having successfully expanded its own cognitive architecture.\n';export{e as default};
