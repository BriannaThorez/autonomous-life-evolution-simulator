const e="# Rendering System Template\n\n## Intent Layer\n- **Axiomatic:** The renderer must maintain deterministic, quantized state transitions (instanced SDF, data texture packing) so that every visual output is a reproducible projection of the cognitive data.\n- **Axiological:** The system should feel bleeding-edge by blending maximal throughput (instanced + batching) with cinematic fidelity (sensory overlays, meatball fusion, shapely fauna/flora).\n\n## Current Rendering Stack\n1. **Data Textures:** `orgTexture` & `floraTexture` hold every organism/flora attribute in contiguous `Float32Array` rows (positions, color, ranges, mates). Describe layout and decode logic in sections.\n2. **Instanced Pipelines:** GFX-005 establishes `FAUNA_VERT_SHADER`/`FLORA_VERT_SHADER` SDF shaders plus `drawFaunaBatches`/`drawFloraBatches`. Document how attributes map to glyphs and the shader math for jiggle, glow, and overlays.\n3. **Culling & Batching:** Phase 2 added frustum+tile culling and calcuated batches. Explain how tiles are computed, how `MAX_INSTANCES` is enforced, and how the reuse of the instance ID buffer keeps the draw loop constant-time.\n4. **Sensory/Meta Effects:** summarize vision/hearing/communication gating, jiggle impulse plan, and density-limited overlays that uphold the balance between perf and spectacle.\n\n## Template Sections\n- **Pack Layout:** Table listing each texture row, semantics (pos, color, velocity, ranges), typical units, and decode steps.\n- **Draw Passes:** Narrate Pass 1 Terrain, Pass 2 Fauna, Pass 3 Flora; include batching logic from `render()` and how uniforms like `u_maxInstances`, `u_posDecode`, `u_zoom` are set.\n- **Performance Knobs:** List `MAX_INSTANCES`, culling thresholds, scale/offset, quantization plans (refer to GFX-006 for future half-float), etc.\n- **Visual Behaviors:** Document SDF primitives, outline glow, meatball fusion gating. Mention Phase 3 jiggle plan and Phase 4 DNA variation as upcoming docs.\n- **Verification:** Provide guidance (Tripartite Verification) on timing, banking draws, and how to confirm `drawFaunaBatches` outputs match expected counts.\n\nUse this file to write future docs that explain the renderer's guts for the team and any future contributors.\r\n";export{e as default};
