const e="# Jelly Jiggle & Body Physics Validation Procedure\n\n## Objective\nEnsure the event-driven jelly jiggle and body-physics behaviors remain perceptible and mathematically stable at the current simulation pacing (30 s/day, ~75 ticks/hour) without regressing the existing instanced/texture-driven renderer.\n\n## Prerequisites\n- Have the simulation running in development mode so you can open the browser console and the WebGL context.\n- Confirm the latest `rendering/WebGLRenderer.ts`, `entities/Fauna/Physiology/deformation.ts`, and `core/SimulationEngine.ts` files are loaded (they expose the amplitude/timing channels we will monitor).\n- Optionally toggle any `VectorDB` debug views that surface event logs, because jiggle impulses correlate with movement events in the registry.\n\n## Step 1: Validate the GPU jiggle channel\n1. Inspect `rendering/WebGLRenderer.ts`, focusing on:\n   - `computeJiggleEvent()` (lines ~673‑705) to understand how speed deltas + turn vectors produce `candidate`.\n   - `updateJiggleState()` and the `jiggleStates` map to see how impulses decay.\n   - The fragment/vertex shader uniform wiring near lines 953/951 (`u_jiggleDecay`, `u_jiggleThreshold`, `u_jiggleZoomGate`).\n2. During a run, open DevTools and log `window.renderer?.jiggleStates` (the renderer instance is attached in the bootstrap). Observe amplitude entries when organisms accelerate, turn, or stop/start.\n3. Capture a short recording or screenshot with zoom levels above/below `u_jiggleZoomGate` (default 0.4). The effect should be gated: at high zoom you see peri-organism glow; at low zoom (zoom < gate) the effect quiets to avoid noise.\n\n## Step 2: Confirm body-physics durability\n1. Replay a scenario with energetic creatures (high speed + frequent direction changes). Use the gene-driven `FernLogic` to ensure surrounding vegetation is not starving them (the same `lifespan`/`energy` metrics drive stability).\n2. Track `WebGLRenderer.computeJiggleEvent()` inputs for representative organisms by temporarily adding `console.debug` inside the method (guarded by a `window.__debugJiggle` flag). Verify that speed deltas, turns, and direction reversals yield `candidate` values ≥ 0.02 so the shader receives nonzero impulses.\n3. Ensure the renderer writes the impulse back into the fauna texture (look for the `orgData[b5] = jiggleState ? jiggleState.amp : 0.0;` assignment near lines 839‑844). If amplitude is missing, confirm the worker thread still writes the `b5` channel when the velocity changes.\n\n## Step 3: Correlate with perceived visuals\n1. With the game running, select an organism and step through several frames while watching the GPU. The jelly jiggle should manifest as a shimmering outline, trailing stretch, or edge glow (see SDF logic in the shaders around lines 121‑225). Confirm by toggling the `showVision/Communication` overlays to ensure no other effect hides it.\n2. Measure pacing by comparing the organism’s `age` or `energy` readouts against the `jiggle` state; when they accelerate, the jiggle amplitude should spike briefly and decay over ~0.25–0.5 seconds (controlled by `JIGGLE_DECAY`).\n\n## Step 4: Regression guards\n1. Add (or keep) a unit test that instantiates `WebGLRenderer` in headless mode, feeds it a fake organism with a known velocity vector, and asserts that `computeJiggleEvent()` returns a value ≥ 0.02 when the velocity changes direction. This protects against later refactors.\n2. Use the existing roadmap proof on GFX-005 to tie this validation: the jiggle amplitude is part of the `Tripartite Verification` pathway described in `documentation/architecture/rendering-system-doc-template.md`.\n\n## Outcome\nFollow this procedure whenever you update the renderer, organism physics, or their data textures. The end result is a quantifiable bounce (candidate + jitter) that you can track through the shader pipeline, leaving no ambiguity about whether the jelly jiggle and associated body physics are still “active” at live pacing.\n";export{e as default};
